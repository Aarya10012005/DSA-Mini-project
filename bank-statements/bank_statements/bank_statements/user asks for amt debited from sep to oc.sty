user asks for amt debited from sep to oct

oct end balance = x
balance  = balance - cred

5.ask user to give the files where starting date is starting of any quarter of the year

1. check for date format in files convert word format to number format for month'sep

2. take two pointer point it to 2 successive lines on a file, compare them and increment the pointers until a different date is reached that is while (strcmp(date1, date2) == 0) when comes ouside of this loop that means two different dates are observed
therefore, order of dates is determined latest or oldest is determined

3.then sort them in oldest to latest for all files

4.combine them. (unsorted order)





